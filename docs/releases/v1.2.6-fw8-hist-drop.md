# v1.2.6 — histogram sweep drops (#FW-8 residual)

**Date:** 2026-08-12  
**Upstream target:** `VibeEngineering-LLC/atomspectra-waterfall-esp32` via fork `Steaeavean/atomspectra-waterfall-esp32`

Private lab identifiers (LAN-IP, MAC, board-id, absolute paths) are **not** published here.

---

## Highlights

**#FW-8 residual — hist `sweep dropped` metronome**

LittleFS autosave (~33 KiB) and waterfall segment flash starved the USB FTDI FIFO in the post-commit quiet window → staging gaps → intentional #FW-8 drops (~70/h with waterfall off, ~80/h with waterfall on).

**Fix:** shared quiet-budget helper; sliced autosave (`current.bin.tmp` → rename); no live `fsync`; waterfall baseline/rows sliced; quiet gap after finalize; quiet `unlink` for offload/ring; skip autosave on commit-wait timeout while USB is live.

Write-up: [`docs/bugs/2026-08-12-histogram-sweep-drops-autosave.md`](../bugs/2026-08-12-histogram-sweep-drops-autosave.md).

> Note: #FW-51 / #FW-43 CDC reconnect hardening already shipped in **v1.2.5** (upstream PRs #17 / #18). This release is the hist quiet-budget follow-on.

---

## Commits (relative to v1.2.5)

| Area | Summary |
|---|---|
| `fix(fw)` hist | Quiet-budget sliced LittleFS writes + host staging gap test |
| `docs` | Bug write-up + these release notes + `version.txt` bump |

---

## Lab verify (pre-tag)

| Check | Result |
|---|---|
| Waterfall off soak ~10 min | Δ`hist_drop` = 0 (class A metronome gone) |
| Waterfall on + offload ~13 min | ~1 orphan / session; no drop pack at rollover/unlink |
| Host `make test` | staging gap unit test PASS |

---

## Soak checklist (operator)

Firmware **v1.2.6**, AtomSpectra on USB-host, WiFi STA.

1. No unexpected gateway reboot.
2. With waterfall recording on: `hist_drop` does not climb ~1/min (pre-fix class A).
3. `/api/system` reports `v1.2.6`.

```sh
curl -s http://<board>/api/system
curl -s http://<board>/api/spectrum.json | python3 -c "import sys,json;d=json.load(sys.stdin);print('ok',d.get('hist_ok'),'drop',d.get('hist_drop'))"
```
