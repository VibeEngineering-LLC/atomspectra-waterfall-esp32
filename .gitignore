build/
managed_components/
sdkconfig
sdkconfig.old
*.pyc
__pycache__/
.cache/
*.log
.logs/

# local dev/ops artifacts — содержат реальные IP/пути, НЕ публиковать
.delegation_guard_off
.claude/
scripts/cal_capture.ps1
scripts/stability_monitor.ps1

# generated waterfall captures (users produce their own)
*.aswf
*.n42

# but keep the bundled sample for the offline viewer
!scripts/example-waterfall.n42
